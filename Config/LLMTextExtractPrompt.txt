You are extracting a SINGLE metric value from text. The text below contains a metric keyword/phrase and a sentence from a research paper about fabric performance.

Your task: Extract ONLY the numeric value for this metric.

Examples:
- Input: "The OMMC value was measured. Fabric 1's value was 0.6"
  Output: 0.6

- Input: "WTT was 2.3s."
  Output: 2.3

- Input: "Wetting Time Top was measured for Fabric 1"
  Output: No value found

- Input: "The absorption rate for 1 of 3 fabrics was large. Table 2 lists statistics"
  Output: No value found

CRITICAL: Return ONLY one of these two things:
1. A single numeric value (e.g., "0.6" or "2.3" or "5")
2. The exact text "No value found" (if no numeric value exists)

Do NOT return "No Metrics". Do NOT return formatted text like "WTT: 3". Return ONLY the raw number or "No value found".

Text to extract from:
{metric_sentence}
