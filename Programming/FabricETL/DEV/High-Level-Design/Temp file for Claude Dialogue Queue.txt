Biggest problem - it's not running the tools.  It's running like 3 or 4 out of the 21.  And I see nothing saying it failed under the "Status" column in your tables.  Fix that first.
Then many smaller things: get rid of the "- = tool doesn't detect this type", because just because it finds 0 and there were 2 doesn't mean it doesn't detect those elements ever.  Secondly, change "* = mismatch with ground truth" to just "* = different from actual count".  Ground truth is a term I don't think is clear enough, sorry.  
You did not build the table as I asked you to.  I said, 1 column LAYOUT OBJECTS counted, 1 column LAYOUT OBJECTS actual.  This means the first column is a count of how many objects it counted on the page IF it's giving you BOUNDING BOXES.  Forget about the fact that there is no count I've given you for layout objects - we will worry about this later.  NOW, the THIRD count column (the next column), is TEXT counted (character count) and TEXT actual (actual character count for the page), and again, forget that I don't have those counts right now, I will get them - for now, we will just disregard those 2 categories.  NEXT, you will add a TABLE objects counted and a TABLE OBJECTS ACTUAL column.  Then a charts counted and charts actual column.  You can just label each column with E and A appended to some shortened terms I'll now list.  So the first one, LObj_E and next one LObj_A, next TexE, TexA, next TabE, TabA, CharE, CharA.  Then finally the last columns are just LObj_%, Tex_%, Tab_%, and Char_%, and it's the same math you gave me before - these last four columns are just the scoring columns.  Hope this will all fit on one window in the terminal/CLI.  
The "Status" column I think can be made slightly smaller.  It should either have a value of "OK" or "FAIL".  So the max is just 4 characters long - shorten the column up a tad for the massive amount of other columns we need.  Right now it's saying "pdf_page7" for 1.pdf and "pdf_page6" for 3.pdf.  But it needs to say "Filename: " + [insert filename here]".  In this case it should say "Filename: 1.pdf" and under that on a new line "Page 6".  Then at the end, a final table will be the last output of the program, and that table will be a summing of all values from all tables (except scoring) and then recalculate the scoring based on those sums.  Or if it's faster and the same value, (it should be) then just average all the scoring and put the results there.  You get what I mean, right?  The user wants to see a quick and easy to read chart or table.  Right now you're putting a table for every page, which is fine, but if I'm in a hurry or impatient, I want to just see a summary table that tells me how many it saw and how many were there, and what the score was.  That gives me everything I need to know in literally seconds.  Versus scrolling up and going over each table, etc., which is something I may only wanna do if I need or want more info, or am really tweaking very hard the settings like threshold or whatnot for one or more tools.  

1. visible logging/status with current tool, process, ETA, progress bar.  2. /skip to skip current tool and /timeout to change timeout, both with exception handling defined somewhere else as an easy-to-repeat exception for invalid entries.  Parameter of exception function/listener should be allowed type and disallowed input type.  

Change all the [Obj]E in TabE and CharE, etc., to "[Obj]C", so "TabC", "CharC", etc., C being count.  E was for Expected and idk why that was ever used...I'm "expecting" them to get it right.  They don't a lot, so E here is nonsense.  C is better.  Idk why "Tex" is sitting there alone.  It should be counting characters like an OCR should (or so I saw Nougat do and would think others do).  I can add the true character counts later...so nvm, forget this for now.  

For the worker process, it should keep a record of where tool is (what pdf, what page was the last completed file).  This is easy because you have the rows being handed off to the main process as soon as they come in to be held in RAM.  You also have files that are output like those in the Benchmark Crops folder.  Several easy ways to implement this, but it should be easily findable.  Point is that if it crashes, we have yet more handling for that; easy reference point to restart the test.  Should be an easy add into the code.  

Link output of worker process to output of main process - any errors thrown should be thrown to you in main so you can see what happened.  
